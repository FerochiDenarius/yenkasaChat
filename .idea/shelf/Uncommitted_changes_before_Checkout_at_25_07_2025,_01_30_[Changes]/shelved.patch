Index: app/src/main/java/com/example/yenkasachat/ui/MainActivity.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package com.example.yenkasachat.ui\n\nimport android.content.Intent\nimport android.os.Bundle\nimport android.util.Log\nimport android.view.View\nimport android.widget.Button\nimport android.widget.PopupMenu\nimport android.widget.Toast\nimport androidx.appcompat.app.AppCompatActivity\nimport androidx.recyclerview.widget.LinearLayoutManager\nimport androidx.recyclerview.widget.RecyclerView\nimport com.example.yenkasachat.R\nimport com.example.yenkasachat.adapter.UserAdapter\nimport com.example.yenkasachat.model.*\nimport com.example.yenkasachat.network.ApiClient\nimport com.example.yenkasachat.util.SharedPrefs\nimport com.example.yenkasachat.ui.GetChatrooms\nimport com.example.yenkasachat.model.ChatRoom\nimport retrofit2.Call\nimport retrofit2.Callback\nimport retrofit2.Response\n\nclass MainActivity : AppCompatActivity() {\n\n    private lateinit var recyclerViewUsers: RecyclerView\n    private lateinit var userAdapter: UserAdapter\n    private val users = mutableListOf<User>()\n\n    private var token: String? = null\n    private var userId: String? = null\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n\n        token = SharedPrefs.getToken(this)\n        userId = SharedPrefs.getUserId(this)\n\n        Log.d(\"MainActivity\", \"Token: $token\")\n        Log.d(\"MainActivity\", \"UserID: $userId\")\n\n        if (token.isNullOrBlank() || userId.isNullOrBlank()) {\n            Toast.makeText(this, \"Please log in again\", Toast.LENGTH_SHORT).show()\n            startActivity(Intent(this, LoginActivity::class.java))\n            finish()\n            return\n        }\n\n        setupUI()\n        fetchAllUsers()\n\n        findViewById<Button>(R.id.btnLogout).setOnClickListener {\n            SharedPrefs.clearToken(this)\n            SharedPrefs.clearUserId(this)\n            Toast.makeText(this, \"Logged out\", Toast.LENGTH_SHORT).show()\n            startActivity(Intent(this, LoginActivity::class.java))\n            finish()\n        }\n    }\n\n    private fun setupUI() {\n        recyclerViewUsers = findViewById(R.id.recyclerViewUsers)\n        recyclerViewUsers.layoutManager = LinearLayoutManager(this)\n        userAdapter = UserAdapter(users) { selectedUser, anchorView ->\n            showUserOptions(selectedUser, anchorView)\n        }\n\n        recyclerViewUsers.adapter = userAdapter\n\n        findViewById<Button>(R.id.btnContacts).setOnClickListener {\n            startActivity(Intent(this, ContactsActivity::class.java))\n        }\n        findViewById<Button>(R.id.btnChatRooms).setOnClickListener {\n            startActivity(Intent(this, ChatRoomsActivity::class.java))\n        }\n        findViewById<Button>(R.id.btnAccountInfo).setOnClickListener {\n            startActivity(Intent(this, AccountInfoActivity::class.java))\n        }\n        findViewById<Button>(R.id.btnVerify).setOnClickListener {\n            Toast.makeText(this, \"Verification coming soon\", Toast.LENGTH_SHORT).show()\n        }\n        findViewById<Button>(R.id.btnSettings).setOnClickListener {\n            Toast.makeText(this, \"Settings coming soon\", Toast.LENGTH_SHORT).show()\n        }\n    }\n\n    private fun fetchAllUsers() {\n        ApiClient.apiService.getAllUsers(\"Bearer $token\")\n            .enqueue(object : Callback<List<User>> {\n                override fun onResponse(call: Call<List<User>>, response: Response<List<User>>) {\n                    if (response.isSuccessful && response.body() != null) {\n                        val currentUserId = userId ?: return\n                        val filtered = response.body()!!.filter { it._id != currentUserId }\n                        users.clear()\n                        users.addAll(filtered)\n                        userAdapter.notifyDataSetChanged()\n                    } else {\n                        Log.e(\"MainActivity\", \"Failed to load users: ${response.code()}\")\n                        Toast.makeText(\n                            this@MainActivity,\n                            \"Failed to load users\",\n                            Toast.LENGTH_SHORT\n                        ).show()\n                    }\n                }\n\n                override fun onFailure(call: Call<List<User>>, t: Throwable) {\n                    Log.e(\"MainActivity\", \"Network failure\", t)\n                    Toast.makeText(this@MainActivity, \"Error: ${t.message}\", Toast.LENGTH_SHORT)\n                        .show()\n                }\n            })\n    }\n\n    private fun showUserOptions(user: User, anchorView: View) {\n        val popup = PopupMenu(this, anchorView)\n        popup.menuInflater.inflate(R.menu.menu_user_options, popup.menu)\n        popup.setOnMenuItemClickListener { item ->\n            when (item.itemId) {\n                R.id.action_create_chat -> {\n                    createChatRoomWithUser(user.username)\n                    true\n                }\n\n                R.id.action_add_contact -> {\n                    addUserToContacts(user.username)\n                    true\n                }\n\n                R.id.action_continue_chat -> {\n                    openExistingChatRoom(user._id)\n                    true\n                }\n\n                else -> false\n            }\n        }\n        popup.show()\n    }\n\n    private fun createChatRoomWithUser(username: String) {\n        val body = mapOf(\"username\" to username)\n        ApiClient.apiService.createChatRoom(\"Bearer $token\", body)\n            .enqueue(object : Callback<CreateChatRoomResponse> {\n                override fun onResponse(\n                    call: Call<CreateChatRoomResponse>,\n                    response: Response<CreateChatRoomResponse>\n                ) {\n                    if (response.isSuccessful && response.body() != null) {\n                        val roomId = response.body()!!.roomId\n                        startActivity(Intent(this@MainActivity, ChatActivity::class.java).apply {\n                            putExtra(\"roomId\", roomId)\n                        })\n                    } else {\n                        Toast.makeText(\n                            this@MainActivity,\n                            \"Chat room creation failed\",\n                            Toast.LENGTH_LONG\n                        ).show()\n                    }\n                }\n\n                override fun onFailure(call: Call<CreateChatRoomResponse>, t: Throwable) {\n                    Toast.makeText(this@MainActivity, \"Error: ${t.message}\", Toast.LENGTH_SHORT)\n                        .show()\n                }\n            })\n    }\n\n    private fun addUserToContacts(username: String) {\n        ApiClient.apiService.addContact(\"Bearer $token\", mapOf(\"username\" to username))\n            .enqueue(object : Callback<Contact> {\n                override fun onResponse(call: Call<Contact>, response: Response<Contact>) {\n                    val msg =\n                        if (response.isSuccessful) \"Contact added!\" else \"Failed to add contact\"\n                    Toast.makeText(this@MainActivity, msg, Toast.LENGTH_SHORT).show()\n                }\n\n                override fun onFailure(call: Call<Contact>, t: Throwable) {\n                    Toast.makeText(this@MainActivity, \"Error: ${t.message}\", Toast.LENGTH_SHORT)\n                        .show()\n                }\n            })\n    }\n\n    // ... (your existing code up to the end of `openExistingChatRoom()`)\n\n    private fun openExistingChatRoom(recipientId: String) {\n        GetChatrooms.fetchChatrooms(this) { chatRooms ->\n            if (chatRooms != null) {\n                val chatRoom = chatRooms.find { room ->\n                    room.participant.id == recipientId\n                }\n\n                if (chatRoom != null && chatRoom.roomId.isNotEmpty()) {\n                    Log.d(\"MainActivity\", \"Opening chat room: ${chatRoom.roomId}\")\n                    startActivity(\n                        Intent(this@MainActivity, ChatActivity::class.java).apply {\n                            putExtra(\"roomId\", chatRoom.roomId)\n                        }\n                    )\n                } else {\n                    Log.w(\"MainActivity\", \"No chat room found for user $recipientId\")\n                    Toast.makeText(\n                        this@MainActivity,\n                        \"No previous chat found\",\n                        Toast.LENGTH_SHORT\n                    ).show()\n                }\n            } else {\n                Toast.makeText(\n                    this@MainActivity,\n                    \"Failed to fetch chat rooms\",\n                    Toast.LENGTH_SHORT\n                ).show()\n            }\n        }\n    }\n}\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/java/com/example/yenkasachat/ui/MainActivity.kt b/app/src/main/java/com/example/yenkasachat/ui/MainActivity.kt
--- a/app/src/main/java/com/example/yenkasachat/ui/MainActivity.kt	(revision e9556827fd38736e964638c6de2cc2ab1f6791f2)
+++ b/app/src/main/java/com/example/yenkasachat/ui/MainActivity.kt	(date 1753404978726)
@@ -129,7 +129,16 @@
                 }
 
                 R.id.action_continue_chat -> {
-                    openExistingChatRoom(user._id)
+                    openExistingChatRoom(user._id) { roomId ->
+                        if (roomId != null) {
+                            startActivity(Intent(this, ChatActivity::class.java).apply {
+                                putExtra("roomId", roomId)
+                            })
+                        } else {
+                            Toast.makeText(this, "No previous chat found", Toast.LENGTH_SHORT).show()
+                        }
+                    }
+
                     true
                 }
 
@@ -186,35 +195,24 @@
 
     // ... (your existing code up to the end of `openExistingChatRoom()`)
 
-    private fun openExistingChatRoom(recipientId: String) {
-        GetChatrooms.fetchChatrooms(this) { chatRooms ->
-            if (chatRooms != null) {
-                val chatRoom = chatRooms.find { room ->
-                    room.participant.id == recipientId
-                }
+    private fun openExistingChatRoom(recipientId: String, callback: (String?) -> Unit) {
+        GetChatrooms.fetchChatrooms(this) { chatrooms ->
+            if (chatrooms == null) {
+                Log.e("openExistingChatRoom", "Failed to fetch chatrooms.")
+                callback(null)
+                return@fetchChatrooms
+            }
 
-                if (chatRoom != null && chatRoom.roomId.isNotEmpty()) {
-                    Log.d("MainActivity", "Opening chat room: ${chatRoom.roomId}")
-                    startActivity(
-                        Intent(this@MainActivity, ChatActivity::class.java).apply {
-                            putExtra("roomId", chatRoom.roomId)
-                        }
-                    )
-                } else {
-                    Log.w("MainActivity", "No chat room found for user $recipientId")
-                    Toast.makeText(
-                        this@MainActivity,
-                        "No previous chat found",
-                        Toast.LENGTH_SHORT
-                    ).show()
-                }
+            // ✅ Find the chatroom with the recipient
+            val existingRoom = chatrooms.find { it.participant.id == recipientId }
+
+            if (existingRoom != null) {
+                Log.d("openExistingChatRoom", "Found existing room: ${existingRoom._id}")
+                callback(existingRoom._id)
             } else {
-                Toast.makeText(
-                    this@MainActivity,
-                    "Failed to fetch chat rooms",
-                    Toast.LENGTH_SHORT
-                ).show()
+                Log.d("openExistingChatRoom", "No existing room with recipient $recipientId")
+                callback(null)
             }
         }
     }
-}
+}
\ No newline at end of file
Index: app/src/main/res/layout/item_user.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+><?xml version=\"1.0\" encoding=\"utf-8\"?>\n<LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    android:orientation=\"horizontal\"\n    android:padding=\"12dp\"\n    android:gravity=\"center_vertical\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"wrap_content\"\n    android:background=\"?android:attr/selectableItemBackground\">\n\n    <ImageView\n        android:id=\"@+id/imageProfile\"\n        android:layout_width=\"48dp\"\n        android:layout_height=\"48dp\"\n        android:layout_marginEnd=\"12dp\"\n        android:scaleType=\"centerCrop\"\n        android:src=\"@drawable/ic_profile_placeholder\"\n        android:contentDescription=\"Profile Picture\" />\n\n    <LinearLayout\n        android:orientation=\"vertical\"\n        android:layout_width=\"0dp\"\n        android:layout_height=\"wrap_content\"\n        android:layout_weight=\"1\">\n\n        <TextView\n            android:id=\"@+id/textUsername\"\n            android:textSize=\"16sp\"\n            android:textStyle=\"bold\"\n            android:layout_width=\"wrap_content\"\n            android:layout_height=\"wrap_content\" />\n\n        <TextView\n            android:id=\"@+id/textLocation\"\n            android:textSize=\"14sp\"\n            android:textColor=\"#666\"\n            android:layout_width=\"wrap_content\"\n            android:layout_height=\"wrap_content\" />\n    </LinearLayout>\n</LinearLayout>\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/res/layout/item_user.xml b/app/src/main/res/layout/item_user.xml
--- a/app/src/main/res/layout/item_user.xml	(revision e9556827fd38736e964638c6de2cc2ab1f6791f2)
+++ b/app/src/main/res/layout/item_user.xml	(date 1753404611953)
@@ -1,39 +1,63 @@
 <?xml version="1.0" encoding="utf-8"?>
 <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
+    xmlns:tools="http://schemas.android.com/tools"
     android:orientation="horizontal"
-    android:padding="12dp"
+    android:padding="16dp"
     android:gravity="center_vertical"
     android:layout_width="match_parent"
     android:layout_height="wrap_content"
     android:background="?android:attr/selectableItemBackground">
 
+    <!-- Profile Image -->
     <ImageView
         android:id="@+id/imageProfile"
-        android:layout_width="48dp"
-        android:layout_height="48dp"
-        android:layout_marginEnd="12dp"
+        android:layout_width="56dp"
+        android:layout_height="56dp"
+        android:layout_marginEnd="16dp"
         android:scaleType="centerCrop"
-        android:src="@drawable/ic_profile_placeholder"
-        android:contentDescription="Profile Picture" />
+        android:src="@drawable/default_avatar"
+        android:contentDescription="@string/profile_picture"
+        tools:ignore="ContentDescription" />
 
+    <!-- Main Content Container -->
     <LinearLayout
         android:orientation="vertical"
         android:layout_width="0dp"
         android:layout_height="wrap_content"
         android:layout_weight="1">
 
+        <!-- Username -->
         <TextView
             android:id="@+id/textUsername"
+            android:layout_width="match_parent"
+            android:layout_height="wrap_content"
             android:textSize="16sp"
             android:textStyle="bold"
-            android:layout_width="wrap_content"
-            android:layout_height="wrap_content" />
+            android:ellipsize="end"
+            android:maxLines="1"
+            tools:text="John Doe" />
 
+        <!-- Last Message -->
         <TextView
-            android:id="@+id/textLocation"
+            android:id="@+id/textLastMessage"
+            android:layout_width="match_parent"
+            android:layout_height="wrap_content"
             android:textSize="14sp"
-            android:textColor="#666"
+            android:textColor="#666666"
+            android:ellipsize="end"
+            android:maxLines="1"
+            android:layout_marginTop="4dp"
+            tools:text="Hello there! How are you?" />
+
+        <!-- Timestamp -->
+        <TextView
+            android:id="@+id/textTimestamp"
             android:layout_width="wrap_content"
-            android:layout_height="wrap_content" />
+            android:layout_height="wrap_content"
+            android:textSize="12sp"
+            android:textColor="#999999"
+            android:layout_marginTop="4dp"
+            android:layout_gravity="end"
+            tools:text="2:30 PM" />
     </LinearLayout>
-</LinearLayout>
+</LinearLayout>
\ No newline at end of file
Index: app/src/main/res/values/strings.xml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+><resources>\n    <string name=\"app_name\">yenkasaChat</string>\n</resources>
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
--- a/app/src/main/res/values/strings.xml	(revision e9556827fd38736e964638c6de2cc2ab1f6791f2)
+++ b/app/src/main/res/values/strings.xml	(date 1753404487913)
@@ -1,3 +1,4 @@
 <resources>
     <string name="app_name">yenkasaChat</string>
+    <string name="profile_picture" />
 </resources>
\ No newline at end of file
Index: app/src/main/java/com/example/yenkasachat/ui/GetChatrooms.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package com.example.yenkasachat.ui\n\nimport android.content.Context\nimport android.util.Log\nimport com.example.yenkasachat.model.ChatRoomResponse\nimport com.example.yenkasachat.network.ApiClient\nimport com.example.yenkasachat.util.SharedPrefs\nimport retrofit2.Call\nimport retrofit2.Callback\nimport retrofit2.Response\n\nobject GetChatrooms {\n\n    fun fetchChatrooms(context: Context, onResult: (List<ChatRoomResponse>?) -> Unit) {\n        val token = SharedPrefs.getToken(context)\n\n        if (token.isNullOrEmpty()) {\n            Log.e(\"ChatroomFetch\", \"No token found.\")\n            onResult(null)\n            return\n        }\n\n        ApiClient.apiService.getChatrooms(\"Bearer $token\")\n            .enqueue(object : Callback<List<ChatRoomResponse>> {\n                override fun onResponse(\n                    call: Call<List<ChatRoomResponse>>,\n                    response: Response<List<ChatRoomResponse>>\n                ) {\n                    if (response.isSuccessful) {\n                        onResult(response.body())\n                    } else {\n                        Log.e(\"ChatroomFetch\", \"Unsuccessful: ${response.code()}\")\n                        onResult(null)\n                    }\n                }\n\n                override fun onFailure(call: Call<List<ChatRoomResponse>>, t: Throwable) {\n                    Log.e(\"ChatroomFetch\", \"Failure: ${t.message}\")\n                    onResult(null)\n                }\n            })\n    }\n}\n
===================================================================
diff --git a/app/src/main/java/com/example/yenkasachat/ui/GetChatrooms.kt b/app/src/main/java/com/example/yenkasachat/ui/GetChatrooms.kt
--- a/app/src/main/java/com/example/yenkasachat/ui/GetChatrooms.kt	(revision e9556827fd38736e964638c6de2cc2ab1f6791f2)
+++ b/app/src/main/java/com/example/yenkasachat/ui/GetChatrooms.kt	(date 1753404748602)
@@ -2,6 +2,7 @@
 
 import android.content.Context
 import android.util.Log
+import androidx.appcompat.app.AppCompatActivity
 import com.example.yenkasachat.model.ChatRoomResponse
 import com.example.yenkasachat.network.ApiClient
 import com.example.yenkasachat.util.SharedPrefs
@@ -11,33 +12,134 @@
 
 object GetChatrooms {
 
-    fun fetchChatrooms(context: Context, onResult: (List<ChatRoomResponse>?) -> Unit) {
-        val token = SharedPrefs.getToken(context)
+    // Cache to prevent duplicate network calls
+    private var lastFetchTime: Long = 0
+    private const val CACHE_TIME_MS = 30000 // 30 seconds
 
-        if (token.isNullOrEmpty()) {
-            Log.e("ChatroomFetch", "No token found.")
-            onResult(null)
+    fun fetchChatrooms(
+        context: Context,
+        forceRefresh: Boolean = false,
+        callback: (List<ChatRoomResponse>?) -> Unit
+    ) {
+        val currentTime = System.currentTimeMillis()
+        if (!forceRefresh && currentTime - lastFetchTime < CACHE_TIME_MS) {
+            Log.d("GetChatrooms", "🔄 Using cached chatrooms")
+            return
+        }
+
+        val token = SharedPrefs.getToken(context) ?: run {
+            Log.e("GetChatrooms", "🔴 No token available")
+            safeCallback(context, callback, null)
             return
         }
 
+        Log.d("GetChatrooms", "🔍 Fetching chatrooms...")
+
         ApiClient.apiService.getChatrooms("Bearer $token")
             .enqueue(object : Callback<List<ChatRoomResponse>> {
                 override fun onResponse(
                     call: Call<List<ChatRoomResponse>>,
                     response: Response<List<ChatRoomResponse>>
                 ) {
-                    if (response.isSuccessful) {
-                        onResult(response.body())
-                    } else {
-                        Log.e("ChatroomFetch", "Unsuccessful: ${response.code()}")
-                        onResult(null)
+                    when {
+                        response.isSuccessful -> {
+                            response.body()?.let { rooms ->
+                                lastFetchTime = currentTime
+                                validateRooms(rooms)
+                                safeCallback(context, callback, rooms)
+                            } ?: run {
+                                Log.e("GetChatrooms", "⚡ Empty response body")
+                                safeCallback(context, callback, null)
+                            }
+                        }
+                        response.code() == 401 -> handleAuthError(context, callback)
+                        else -> handleApiError(response, context, callback)
                     }
                 }
 
                 override fun onFailure(call: Call<List<ChatRoomResponse>>, t: Throwable) {
-                    Log.e("ChatroomFetch", "Failure: ${t.message}")
-                    onResult(null)
+                    Log.e("GetChatrooms", "🌐 Network failure", t)
+                    safeCallback(context, callback, null)
                 }
             })
     }
-}
+
+    private fun validateRooms(rooms: List<ChatRoomResponse>) {
+        if (rooms.isEmpty()) {
+            Log.d("GetChatrooms", "ℹ️ No chat rooms found")
+            return
+        }
+
+        rooms.forEachIndexed { index, room ->
+            when {
+                room._id.isNullOrEmpty() ->
+                    Log.w("GetChatrooms", "Room at index $index has null ID")
+                room.participant.id.isNullOrEmpty() ->
+                    Log.w("GetChatrooms", "Room ${room._id} has invalid participant")
+            }
+        }
+    }
+
+    private fun handleAuthError(
+        context: Context,
+        callback: (List<ChatRoomResponse>?) -> Unit
+    ) {
+        Log.e("GetChatrooms", "🔒 Authentication failed")
+        (context as? AppCompatActivity)?.run {
+            // Trigger logout flow or token refresh
+            // Example: startActivity(Intent(this, LoginActivity::class.java))
+        }
+        safeCallback(context, callback, null)
+    }
+
+    private fun handleApiError(
+        response: Response<List<ChatRoomResponse>>,
+        context: Context,
+        callback: (List<ChatRoomResponse>?) -> Unit
+    ) {
+        val errorCode = response.code()
+        val errorBody = response.errorBody()?.string() ?: "No error details"
+        Log.e("GetChatrooms", "❌ API Error $errorCode: $errorBody")
+
+        when (errorCode) {
+            500 -> Log.e("GetChatrooms", "Server error - try again later")
+            404 -> Log.e("GetChatrooms", "Endpoint not found - check API version")
+            else -> Log.e("GetChatrooms", "Unhandled API error")
+        }
+
+        safeCallback(context, callback, null)
+    }
+
+    private fun safeCallback(
+        context: Context,
+        callback: (List<ChatRoomResponse>?) -> Unit,
+        data: List<ChatRoomResponse>?
+    ) {
+        try {
+            (context as? AppCompatActivity)?.runOnUiThread {
+                callback(data)
+            } ?: callback(data)
+        } catch (e: Exception) {
+            Log.e("GetChatrooms", "Callback failed", e)
+        }
+    }
+
+    // Debug function with enhanced logging
+    fun debugFetch(context: Context) {
+        fetchChatrooms(context) { rooms ->
+            Log.d("GetChatrooms-DEBUG", "=== Chatrooms Debug ===")
+            Log.d("GetChatrooms-DEBUG", "Total rooms: ${rooms?.size ?: 0}")
+
+            rooms?.forEachIndexed { index, room ->
+                Log.d("GetChatrooms-DEBUG", """
+                    Room #${index + 1}:
+                    ID: ${room._id}
+                    Participant: ${room.participant.id} (${room.participant.username})
+                    Last Message: ${room.lastMessage?.text?.take(30) ?: "None"}
+                    Last Updated: ${room.lastMessageAt ?: "Never"}
+                    ======================
+                """.trimIndent())
+            } ?: Log.d("GetChatrooms-DEBUG", "Null rooms received")
+        }
+    }
+}
\ No newline at end of file
Index: app/src/main/java/com/example/yenkasachat/model/ChatRoomResponse.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package com.example.yenkasachat.model\n\ndata class ChatRoomResponse(\n    val roomId: String,\n    val participant: Participant,\n    val lastMessage: LastMessage?,\n    val lastMessageType: String?,\n    val lastMessageAt: String?\n)\n\ndata class Participant(\n    val id: String,\n    val username: String,\n    val avatar: String?\n)\n\ndata class LastMessage(\n    val text: String?,\n    val imageUrl: String?,\n    val audioUrl: String?,\n    val videoUrl: String?,\n    val fileUrl: String?,\n    val contactInfo: String?,\n    val location: String?,\n    val createdAt: String\n)\n
===================================================================
diff --git a/app/src/main/java/com/example/yenkasachat/model/ChatRoomResponse.kt b/app/src/main/java/com/example/yenkasachat/model/ChatRoomResponse.kt
--- a/app/src/main/java/com/example/yenkasachat/model/ChatRoomResponse.kt	(revision e9556827fd38736e964638c6de2cc2ab1f6791f2)
+++ b/app/src/main/java/com/example/yenkasachat/model/ChatRoomResponse.kt	(date 1753405232574)
@@ -1,16 +1,26 @@
 package com.example.yenkasachat.model
 
+import com.google.gson.annotations.SerializedName
+
+
+// Update to match backend EXACTLY
 data class ChatRoomResponse(
-    val roomId: String,
-    val participant: Participant,
+    @SerializedName("_id") val _id: String,
+    @SerializedName("roomId") val roomId: String? = null, // Handle both fields
+    val participant: Participant, // SINGULAR (matches backend)
     val lastMessage: LastMessage?,
-    val lastMessageType: String?,
     val lastMessageAt: String?
-)
+) {
+    // Helper to get consistent ID
+    fun getRoomId() = roomId ?: _id
+}
 
 data class Participant(
+    @SerializedName("_id") // ✅ If this is a MongoDB ID from backend
     val id: String,
+
     val username: String,
+
     val avatar: String?
 )
 
@@ -22,5 +32,7 @@
     val fileUrl: String?,
     val contactInfo: String?,
     val location: String?,
+
+    @SerializedName("createdAt")
     val createdAt: String
 )
