<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/feed_background"
    tools:context=".ui.MainActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/mainAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/feed_surface"
        android:elevation="0dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:background="@color/feed_surface"
            app:navigationIcon="@null"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            app:titleTextColor="@color/feed_primary_text">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <ImageView
                android:id="@+id/btnMenu"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_centerVertical="true"
                android:background="@drawable/bg_feed_icon_button"
                android:contentDescription="@string/menu"
                android:padding="8dp"
                android:src="@drawable/ic_menu"
                app:tint="@color/feed_primary_text" />

            <LinearLayout
                android:id="@+id/toolbarBrand"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:layout_toEndOf="@id/btnMenu"
                android:layout_toStartOf="@id/topBarActions"
                android:gravity="center"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:contentDescription="@string/yenkasa_brand"
                    android:src="@drawable/ic_logo_emblem" />

                <TextView
                    android:id="@+id/toolbarTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:ellipsize="end"
                    android:fontFamily="sans-serif-medium"
                    android:includeFontPadding="false"
                    android:maxLines="1"
                    android:singleLine="true"
                    android:text="@string/yenkasa_brand"
                    android:textColor="@color/feed_primary_text"
                    android:textSize="18sp" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/topBarActions"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:gravity="center"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/btnNotifications"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:background="@drawable/bg_feed_icon_button"
                    android:contentDescription="@string/notifications"
                    android:padding="8dp"
                    android:src="@drawable/ic_notification"
                    app:tint="@color/feed_primary_text" />

                <LinearLayout
                    android:id="@+id/btnCreateAd"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_feed_create_ad"
                    android:gravity="center"
                    android:minWidth="82dp"
                    android:orientation="horizontal"
                    android:paddingHorizontal="12dp">

                    <ImageView
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:src="@drawable/ic_ads_plus"
                        app:tint="@color/feed_action_icon" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="6dp"
                        android:includeFontPadding="false"
                        android:text="@string/create_ad"
                        android:textColor="@color/feed_action_icon"
                        android:textSize="12sp"
                        android:textStyle="bold" />
                </LinearLayout>
            </LinearLayout>

        </RelativeLayout>
    </androidx.appcompat.widget.Toolbar>

</com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:id="@+id/feedContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnYenkasaLive"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:layout_marginEnd="22dp"
        android:layout_marginBottom="102dp"
        android:contentDescription="@string/browse_live_streams"
        android:elevation="8dp"
        app:backgroundTint="@color/feed_action_background"
        app:borderWidth="0dp"
        app:layout_anchor="@id/feedContainer"
        app:layout_anchorGravity="bottom|end"
        app:srcCompat="@drawable/ic_live_bolt"
        app:tint="@color/feed_action_icon"
        app:useCompatPadding="true" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabCreatePost"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginEnd="28dp"
        android:layout_marginBottom="14dp"
        android:contentDescription="@string/create_post_label"
        android:elevation="8dp"
        app:backgroundTint="@color/feed_action_background"
        app:borderWidth="0dp"
        app:fabCustomSize="44dp"
        app:layout_anchor="@id/feedContainer"
        app:layout_anchorGravity="bottom|end"
        app:maxImageSize="22dp"
        app:srcCompat="@drawable/ic_add"
        app:tint="@color/feed_action_icon"
        app:useCompatPadding="true" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
